<Window x:Class="Bewerbungstracker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:local="clr-namespace:Bewerbungstracker"
        mc:Ignorable="d"
        Title="Bewerbungstracker - 6 Monate Praktikum" 
        Height="650" Width="800"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Titel -->
            <RowDefinition Height="Auto"/>
            <!-- Eingabe -->
            <RowDefinition Height="Auto"/>
            <!-- Aktive Box -->
            <RowDefinition Height="Auto"/>
            <!-- Absagen Box -->
            <RowDefinition Height="Auto"/>
            <!-- Statusleiste -->
        </Grid.RowDefinitions>

        <!-- Titel -->
        <TextBlock Grid.Row="0" Text="📋 Meine Bewerbungen" FontSize="22" FontWeight="Bold" Margin="0,0,0,15"/>

        <!-- Eingabebereich -->
        <Border Grid.Row="1" BorderBrush="LightGray" BorderThickness="1" Padding="10" CornerRadius="8" Margin="0,0,0,15">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Label Grid.Row="0" Grid.Column="0" Content="Firma:" FontWeight="Bold"/>
                    <TextBox Grid.Row="0" Grid.Column="1" x:Name="txtFirma" Height="30" Margin="0,0,0,10"/>

                    <Label Grid.Row="1" Grid.Column="0" Content="Plattform:" FontWeight="Bold"/>
                    <ComboBox Grid.Row="1" Grid.Column="1" x:Name="cbPlattform" Height="30" Margin="0,0,0,10">
                        <ComboBoxItem Content="LinkedIn"/>
                        <ComboBoxItem Content="Indeed"/>
                        <ComboBoxItem Content="Xing"/>
                        <ComboBoxItem Content="StepStone"/>
                        <ComboBoxItem Content="Direkt"/>
                        <ComboBoxItem Content="Andere"/>
                    </ComboBox>

                    <Label Grid.Row="2" Grid.Column="0" Content="Datum:" FontWeight="Bold"/>
                    <DatePicker Grid.Row="2" Grid.Column="1" x:Name="dpDatum" SelectedDate="2026-04-17" Margin="0,0,0,10"/>

                    <Label Grid.Row="3" Grid.Column="0" Content="Status:" FontWeight="Bold"/>
                    <ComboBox Grid.Row="3" Grid.Column="1" x:Name="cbStatus" Height="30">
                        <ComboBoxItem Content="📧 Beworben"/>
                        <ComboBoxItem Content="⏳ Warte auf Rückmeldung"/>
                        <ComboBoxItem Content="✅ Einladung zum Gespräch"/>
                        <ComboBoxItem Content="🎉 Zusage bekommen"/>
                        <ComboBoxItem Content="❌ Absage"/>
                    </ComboBox>
                </Grid>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">
                    <Button x:Name="btnPruefen" Content="🔍 Firma prüfen" Width="120" Height="35" Margin="5" Click="BtnPruefen_Click"/>
                    <Button x:Name="btnSpeichern" Content="💾 Bewerbung speichern" Width="140" Height="35" Margin="5" Click="BtnSpeichern_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Aktive Bewerbungen Box -->
        <Border Grid.Row="2" BorderBrush="Green" BorderThickness="2" Padding="5" CornerRadius="8" Margin="0,0,0,10" Background="#F0FFF0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="📋 AKTIVE BEWERBUNGEN (offen / warte / Gespräch / Zusage)" FontWeight="Bold" FontSize="14" Margin="5" Foreground="DarkGreen"/>
                <ListBox Grid.Row="1" x:Name="lstAktive" Height="180" DisplayMemberPath="DisplayText" SelectionChanged="LstAktive_SelectionChanged"/>
            </Grid>
        </Border>

        <!-- Absagen Box -->
        <Border Grid.Row="3" BorderBrush="Red" BorderThickness="2" Padding="5" CornerRadius="8" Margin="0,0,0,10" Background="#FFF0F0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="❌ ABSAGEN (nicht gelöscht - nur archiviert)" FontWeight="Bold" FontSize="14" Margin="5" Foreground="DarkRed"/>
                <ListBox Grid.Row="1" x:Name="lstAbsagen" Height="120" DisplayMemberPath="DisplayText"/>
            </Grid>
        </Border>

        <!-- Statusleiste und Buttons -->
        <StatusBar Grid.Row="4" Margin="0,10,0,0">
            <StatusBarItem>
                <StackPanel Orientation="Horizontal">
                    <TextBlock x:Name="txtStatus" Text="Bereit" Width="400"/>
                    <Button x:Name="btnLoeschen" Content="🗑 Ausgewählte löschen" Width="150" Height="25" Margin="20,0,0,0" Click="BtnLoeschen_Click" Visibility="Collapsed"/>
                </StackPanel>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>